const config = {
	content: ['./src/**/*.{html,js,svelte,ts}'],

	theme: {
		extend: {},
		fontFamily: {
			display: ['Cal Sans'],
			body: ['"Inter"']
		}
	},

	plugins: []
};

module.exports = config;
